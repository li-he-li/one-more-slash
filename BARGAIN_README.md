# 砍价大厅页面实现文档

## 📁 项目结构

```
duoduo/
├── app/
│   ├── bargain/
│   │   └── page.tsx          # 砍价大厅页面
│   ├── layout.tsx            # 根布局
│   └── globals.css           # 全局样式
├── components/
│   └── ProductCard.tsx       # 商品卡片组件
├── styles/
│   └── bargain.scss          # SCSS 样式文件（可选）
├── tailwind.config.ts        # Tailwind 配置（已更新）
└── package.json
```

## 🎨 设计实现

### 1. 配色方案（拼多多风格）

已添加到 `tailwind.config.ts`：

```typescript
colors: {
  primary: {
    DEFAULT: '#E54C3C',      // 主红色
    light: '#FF6B6B',         // 浅红色
    bg: '#FFE5E5',            // 背景浅红
  },
  text: {
    primary: '#333333',       // 主要文字
    secondary: '#666666',     // 次要文字
    light: '#999999',         // 浅色文字
  },
  bg: {
    card: '#FFFFFF',          // 卡片背景
    page: '#F5F5F5',          // 页面背景
  },
}
```

### 2. 页面布局

**2×4 网格布局**（2行4列）：
- 每行显示 4 个商品卡片
- 共 8 个商品示例
- 响应式设计，自动适配屏幕

### 3. 组件结构

#### 导航栏
- Logo "多多砍价"
- 搜索框
- 导航链接（首页、砍价大厅、我的砍价、发布商品）
- 账号设置按钮

#### 商品卡片
- 商品图片占位区
- 商品标题
- 砍价进度条
- 价格信息（已砍/目标）
- 发布价（删除线）
- 用户信息（头像+昵称）
- 剩余时间
- "帮他砍一刀"按钮

## 🚀 使用方式

### 方式 1：使用 Tailwind CSS（推荐）

页面已经使用 Tailwind CSS 实现，直接访问：

```
http://localhost:3000/bargain
```

### 方式 2：集成 SCSS

如果你想使用 SCSS 样式：

1. 安装依赖：
```bash
npm install --save-dev sass
```

2. 在 `app/bargain/page.tsx` 中导入：
```typescript
import '@/styles/bargain.scss';
```

3. 或者添加到 `app/globals.css`：
```css
@import '@/styles/bargain.scss';
```

## 📦 组件使用示例

### ProductCard 组件

```tsx
import { ProductCard } from '@/components/ProductCard';

<ProductCard
  title="Apple iPhone 15 Pro Max 256GB"
  progress={180}
  currentPrice="¥5,980"
  targetPrice="¥6,999"
  publishPrice="¥7,999"
  user="小红正在砍价"
  timeLeft="23:45:12"
/>
```

### Props 说明

| Prop | 类型 | 说明 |
|------|------|------|
| title | string | 商品标题 |
| progress | number | 进度条宽度（像素） |
| currentPrice | string | 已砍金额 |
| targetPrice | string | 目标金额 |
| publishPrice | string | 发布价 |
| user | string | 用户昵称 |
| timeLeft | string | 剩余时间 |

## 🎯 样式定制

### 修改配色

编辑 `tailwind.config.ts` 中的 `theme.extend.colors`：

```typescript
colors: {
  primary: {
    DEFAULT: '#YOUR_COLOR',
  },
}
```

### 修改布局

在 `app/bargain/page.tsx` 中修改网格：

```tsx
// 2行4列
<div className="grid grid-cols-4 gap-4">

// 改为 3行3列
<div className="grid grid-cols-3 gap-4">
```

## 🔧 开发命令

```bash
# 启动开发服务器
npm run dev

# 构建生产版本
npm run build

# 启动生产服务器
npm start

# 代码检查
npm run lint
```

## 📱 响应式设计

页面已实现响应式布局：

- **桌面端**（>1280px）：4列
- **平板端**（1024-1280px）：3列
- **手机端**（640-1024px）：2列
- **小屏手机**（<640px）：1列

## 🎨 特性

✅ 完全响应式设计
✅ 拼多多风格配色
✅ 平滑动画过渡
✅ 组件化结构
✅ TypeScript 类型安全
✅ Tailwind CSS 原子化样式
✅ 可选的 SCSS 模块化样式

## 📝 下一步

1. 连接后端 API
2. 添加数据加载状态
3. 实现搜索功能
4. 添加筛选和排序
5. 实现分页加载
6. 添加用户认证
7. 实现砍价交互功能
