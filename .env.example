# SecondMe API 配置
SECONDME_APP_ID=your_secondme_app_id
SECONDME_APP_SECRET=your_secondme_app_secret
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your_nextauth_secret

# 数据库配置
DATABASE_URL="file:./dev.db"

# Vercel Blob Storage 配置（用于图片上传）
# 在 Vercel 部署时，需要在 Vercel 项目设置中创建 Blob Store
# 本地开发时，如果需要测试图片上传功能，也需要配置此变量
BLOB_READ_WRITE_TOKEN=vercel_blob_rw_xxxxxxxxxxxxx

# 如何获取 BLOB_READ_WRITE_TOKEN：
# 1. 访问 https://vercel.com/docs/storage/vercel-blob
# 2. 在 Vercel 项目中创建 Blob Store
# 3. 在项目设置的环境变量中找到 BLOB_READ_WRITE_TOKEN
# 4. 复制该 token 并在本地开发时添加到 .env.local 文件中
